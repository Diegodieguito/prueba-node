function showDone( {
    for ()
})




function showPending() {
    for (let i=0; i < TextTrackList.length; i++) {
        const rask = tasks [i] ;
        const taskNotDone = !taskNotDonr.done;
        if (taskNotDone) {
            console.log (`- ok ${task.me} (${task.deadline}))
        }
    }
}

const argumentsArray = process.argv;